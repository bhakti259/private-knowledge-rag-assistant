# Frontend (React + Vite)

This module provides a simple UI for:

- Sending chat messages to the backend
- Triggering ingestion with lightweight document inputs

## Structure

- `src/api` -> API client wrappers
- `src/components` -> UI components per feature
- `src/types` -> shared API DTO types
